<?php /** @var $block Ecomail\Ecomail\Block\System\Config\Form\Field\Status */ ?>
<?php $statusCode = $block->getStatusCode() ?>

<span class="ecomail-status <?= $block->escapeHtmlAttr($statusCode) ?>">
    <?= $block->escapeHtml(__($statusCode)) ?>
</span>
<div class="ecomail-unsaved-warning"
     data-mage-init='{"Ecomail_Ecomail/js/unsaved-config-warning": {}}'
     role="alert"
     aria-live="polite">
    <strong><?= $block->escapeHtml(__('Unsaved changes')) ?></strong>
    <span><?= $block->escapeHtml(__('Save the Ecomail configuration before leaving this page.')) ?></span>
    <button type="button" class="action-default ecomail-unsaved-save">
        <span><?= $block->escapeHtml(__('Save Config')) ?></span>
    </button>
</div>

<style type="text/css">
    .ecomail-status {
        padding: 0.5rem 2rem;
        color: white;
        display: inline-block;
        border-radius: 3px;
    }

    .ecomail-status.Inactive {
        background-color: #a5a6b7;
    }

    .ecomail-status.Active {
        background-color: #52C41a;
    }

    .ecomail-status.Error {
        background-color: #ff4d4f;
    }

    .ecomail-unsaved-warning {
        align-items: center;
        background: #fff8e5;
        border: 1px solid #f0b849;
        border-left: 4px solid #eb5202;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.16);
        color: #514943;
        display: none;
        gap: 10px;
        max-width: 520px;
        padding: 12px 14px;
        position: fixed;
        right: 24px;
        top: 82px;
        z-index: 900;
    }

    .ecomail-unsaved-warning.is-visible {
        display: flex;
    }

    .ecomail-unsaved-warning strong {
        color: #eb5202;
        white-space: nowrap;
    }

    .ecomail-unsaved-warning .ecomail-unsaved-save {
        margin-left: auto;
        white-space: nowrap;
    }

    .ecomail-api-key-control {
        align-items: stretch;
        display: inline-flex;
        max-width: 520px;
        width: 100%;
    }

    .ecomail-api-key-control .ecomail-api-key-input {
        border-bottom-right-radius: 0;
        border-top-right-radius: 0;
        flex: 1 1 auto;
        min-width: 0;
    }

    .ecomail-api-key-control .ecomail-api-key-toggle {
        border-bottom-left-radius: 0;
        border-left: 0;
        border-top-left-radius: 0;
        min-width: 42px;
        padding-left: 10px;
        padding-right: 10px;
    }

    @media (max-width: 900px) {
        .ecomail-unsaved-warning {
            left: 16px;
            max-width: none;
            right: 16px;
            top: 64px;
        }
    }
</style>
